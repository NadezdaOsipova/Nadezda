import com.home.model.Person;

import static com.home.utils.Constants.MIN_AGE_PERSON;
import static com.home.utils.Constants.SEX_PERSON;


public class nest {
    private static final int MAX_AGE_PERSON = ;

    public static void main(String[] args) {

        Person[] person = new Person[]; //массив случайных людей
        int age = 0;
        String sex = "female";
        int k = 0;
        private static final  MAX_AGE_PERSON = 27;
        int k = 0;
        for (int i = 0; i < person.length; i++) {
            if (age >= MIN_AGE_PERSON && MAX_AGE_PERSON >= age) {
                if (sex == SEX_PERSON) {
                    k++;
                }
                return k;
            }

        }

    }
}
